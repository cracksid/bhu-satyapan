# pytest settings for this project.
#
# pythonpath = .   puts the project folder on Python's import path, so a test
#                  can do "from src.validation import ..." while you run
#                  pytest from the project folder.
# testpaths  = tests   means a bare "pytest" only looks in tests/.

[pytest]
pythonpath = .
testpaths = tests
